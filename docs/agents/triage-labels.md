# Triage Labels

The skills speak in terms of five canonical triage roles. This file maps those roles to the actual label strings used in this repo's issue tracker.

| Label in skills | Label in this repo | Meaning |
|---|---|---|
| `needs-triage` | `needs-triage` | Maintainer needs to evaluate this issue |
| `needs-info` | `needs-info` | Waiting on reporter for more information |
| `ready-for-agent` | `ready-for-agent` | Fully specified, ready for an AFK agent |
| `ready-for-human` | `ready-for-human` | Requires human implementation |
| `wontfix` | `wontfix` | Will not be actioned |

Additional module labels used in this repo:

| Label | Meaning |
|---|---|
| `calendar` | Calendar module |
| `reminders` | Reminders module |
| `infra` | Infrastructure / project setup |
| `tdd` | Test-Driven Development task |
| `epic` | Top-level epic |
| `feature` | Feature story |
| `story` | User story / task |

When a skill mentions a triage role (e.g. "apply the AFK-ready label"), use the label string from the left column above.
